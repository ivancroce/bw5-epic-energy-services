import { Navbar, Container } from "react-bootstrap";

const MyNavBar = () => {
  return (
    <Navbar bg="dark" variant="dark">
      <Container>
        <Navbar.Brand>Epic Energy CRM</Navbar.Brand>
      </Container>
    </Navbar>
  );
};

export default MyNavBar;
